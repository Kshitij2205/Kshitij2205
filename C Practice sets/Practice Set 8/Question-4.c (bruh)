// Write a function to slice() a string. It should change the original string such that it is now the sliced string.
// Take m and n as the start and ending position for slice. 

#include <stdio.h>
#include <string.h>

int main()
{
    char *name = "Kshitij";
    int m, n;

    m = name[0];
    n = strlen(name) - 1;

    

    return 0;
}