// Write a program to check whether a number is divisible by 97 or not.

#include <stdio.h>

int main()
{
    int num = 97;

    printf("The remainder is %d.\n", num % 97);

    return 0;
}