// Write a program containing a function which reverses the array passed to it.

#include <stdio.h>

void reverse(int *arr)
{
    int temp = 0;

    for(int i = 0; i <= 1; i++)
    {
        temp = arr[0];
        
    }
}

int main()
{
    int arr[5] = {1, 2, 3, 4, 5};

    reverse(&arr[0]);

    return 0;
}